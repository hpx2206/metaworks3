<%
    var beanPropertyIdx = 0;
    if( value && value.activityList ){
        for(var i in value.activityList){
            var activity = value.activityList[i];
           %>
           <%=mw3.locateObject(activity.activityView , activity.activityView.__className, null, null, {objectId: objectId, name: '[' + beanPropertyIdx++ + ']'})%>
           <%
        }
    }
    if( value && value.valueChainList ){
        for(var i in value.valueChainList){
            var valueChain = value.valueChainList[i];
           %>
           <%=mw3.locateObject(valueChain.valueChainView , valueChain.valueChainView.__className, null, null, {objectId: objectId, name: '[' + beanPropertyIdx++ + ']'})%>
           <%
        }
    }
    if( value && value.roleList ){
        var beanPropertyIdx = 0;
        for(var i in value.roleList){
            var role = value.roleList[i];
           %>
           <%=mw3.locateObject(role.roleView , role.roleView.__className, null, null, {objectId: objectId, name: '[' + beanPropertyIdx++ + ']'})%>
           <%
        }
    }
    if( value && value.transitionList ){
        for(var i in value.transitionList){
            var transition = value.transitionList[i];
            if( transition.__className == 'org.uengine.kernel.graph.PoolTransition'){
            %>
                <%=mw3.locateObject(transition.poolTransitionView , transition.poolTransitionView.__className, null, null, {objectId: objectId, name: '[' + beanPropertyIdx++ + ']'})%>
           <%
            }else{
           %>
                <%=mw3.locateObject(transition.transitionView , transition.transitionView.__className, null, null, {objectId: objectId, name: '[' + beanPropertyIdx++ + ']'})%>
           <%
           }
        }
    }
    if( value && value.poolList ){
        for(var i in value.poolList){
            var pool = value.poolList[i];
           %>
           <%=mw3.locateObject(pool.poolView , pool.poolView.__className, null, null, {objectId: objectId, name: '[' + beanPropertyIdx++ + ']'})%>
           <%
        }
    }
    if( value && value.graphicList ){
        for(var i in value.graphicList){
           %>
           <%=mw3.locateObject(value.graphicList[i] , value.graphicList[i].__className, null, null, {objectId: objectId, name: '[' + beanPropertyIdx++ + ']'})%>
           <%
        }
    }
    if( value && value.graphicLineList ){
        for(var i in value.graphicLineList){
           %>
           <%=mw3.locateObject(value.graphicLineList[i] , value.graphicLineList[i].__className, null, null, {objectId: objectId, name: '[' + beanPropertyIdx++ + ']'})%>
           <%
        }
    }
    if( value && value.processDetailPanel ){
        %>
        <%= fields.processDetailPanel.here() %>
        <%
    }
    if( value && value.processSummaryPanel ){
        %>
        <%= fields.processSummaryPanel.here() %>
        <%
    }
%>