<%
	if(value){
		if(value.competitionNode){
%>
	<ul>
<%
			for (var i=0; i<value.competitionNode.length; i++){
				if(typeof options == 'undefined') 
					options = objectMetadata;
			
				options['index'] = i;
				options['htmlTag'] =  'li';
%>
		<%=mw3.locateObject(value.competitionNode[i], value.competitionNode[i].__className, null, options, {objectId: value.__objectId, name: 'competitionNode[' + i + ']'})%>	
<%
			}
%>
		</ul>
		
			<%
			if(!value.selectedMore) {
			
				if(value.countcompetitionNode >=6) {
			%>
			
				<span class="west_more_btn" style="display:none"><a onClick="<%=methods.moreView.caller()%>" href="#"><%=mw3.localize('$More')%></a></span>
<%
				}
			}else{
%>			
	
				<span class="west_more_btn" style="display:none"><a onClick="<%=methods.load.caller()%>" href="#"><%=mw3.localize('$SimpleView')%></a></span>
					
			
<%			
			}
		}
	}
%>	
