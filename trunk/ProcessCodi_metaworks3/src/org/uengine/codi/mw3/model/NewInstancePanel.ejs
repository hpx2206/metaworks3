<%
	if(value){
		if(mw3.how == 'sns'){			
%>
	<%=fields.newInstantiator.here()%>
<%	
		if(false){
%>		
	<div id="openmind_btn" style="position:absolute; right:15px; top:50px;cursor:pointer;">
		공개설정▼
	</div>
	<div id="openmind_box">
		<ul>
<%
			for(var i=0; i<value.securityLevel.optionValues.length; i++){
				var optionValue = value.securityLevel.optionValues[i];
				var optionName = value.securityLevel.optionNames[i];
%>			
		
			<li value="<%=optionValue%>"><%=mw3.localize(optionName)%></li>
<%
			}
%>			
		</ul>
	</div>

<%		
			}
		}else{
%>
   
   <div class="topic_msg"><span class="toolbarIcon icon-calendar"></span> <span><%=fields.dueDate.here({when: mw3.WHEN_VIEW})%></span><span class="clear"></span></div>
   <div style="height:30px; line-height:30px;"></div>
   
    
	<%=fields.newInstantiator.here()%>
	
<%
	if(value.processMapPanel != null){
%>				
	<div id="wihOrContent" style="width: 100%; text-align: center; font-size: 30px; font-weight: bold; "><%=mw3.localize("$or")%></div>
<%
	}
%>	
	<%=fields.processMapPanel.here()%>
	<div><%=fields.securityLevel.here({when:'edit'})%></div>
<%		
		}
	}
%>
