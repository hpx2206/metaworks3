<?xml version="1.0"?>
<project name="SSH" basedir="." default="start">

	<target name="stop" >
        <sshexec host="${host}"
				 PORT="${port}"
                 username="${id}"
                 password="${pw}"
                 trust="true"
                 command="source /etc/profile; ${bin}/shutdown.sh; rm -rf ${dest}/uengine-web"/>
    </target>
	
	<target name="deploy-war" depends="stop">
		<scp file="${war.file}" todir="${id}:${pw}@${host}:${dest}" port="${port}" trust="true"/>
	</target>

	<target name="start" depends="deploy-war">
        <sshexec host="${host}"
				 PORT="${port}"
                 username="${id}"
                 password="${pw}"
                 trust="true"
                 command="source /etc/profile; sh -x ${bin}/startup.sh"/>
    </target>
</project>