<%

if(mw3.when == mw3.WHEN_EDIT || mw3.when == mw3.WHEN_NEW){

%>
<select id='<%=mw3.createInputId(objectId)%>'>

<%  
	for(var i=0; i<descriptor.options.length; i++){
	
	
%><option value="<%=descriptor.values[i]%>"<%=(value == descriptor.values[i] ? " selected" : "")%>> <%=descriptor.options[i]%>
<%
	}

}else{  //view mode 

	if(value != null){
		for(var i=0; i<descriptor.options.length; i++){
		
			if(value == descriptor.values[i]){
	%><%=descriptor.options[i]%>
	<%
			}
		}
	}else{
%>
NULL
<%
	}

}	
%>