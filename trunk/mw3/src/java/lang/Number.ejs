<%  
 if(value==null || isNaN(value)){
 	value = "0";
  }
	
	if(mw3.when == mw3.WHEN_EDIT || mw3.when == mw3.WHEN_NEW){
%>
	 <input id="<%=mw3.createInputId(objectId)%>" onkeydown="mw3.getFaceHelper('<%=objectId%>').keydown();" value="<%=value%>" style="text-align:right;" <%=descriptor && descriptor.attributes && descriptor.attributes.size ? " size=" + descriptor.attributes.size : ""%>>
	
<%

	}else{
%>
	 <%=value%>
<%
	}
%> 