<%
	if(value){
	
		var option = '';
		
		if(mw3.when != 'new' && mw3.when != 'edit'){
			option = 'style=\"border:none\"';
		} 
%>	
	<input type="text" id="<%=mw3.createInputId(objectId)%>" style="width:80px; color:#666" when="<%=mw3.when%>" <%=option%> readonly />
<%
	}
%>