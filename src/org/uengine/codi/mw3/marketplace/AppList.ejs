<%
	if(value != null && typeof value != 'undefined'){
		if(value.value && value.value.length > 0){
%>

	<%=fields.value.here()%>

<%
		}else{
%>
	<div style="padding: 24px 21px; font-size: 14px; display: block;">
		<div>아직 등록된 앱이 없습니다.</div>
	</div>
<%		
		}
	}
%>
