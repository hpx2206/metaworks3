<%
	if(value){
%>
		<ul>
<%
		for (var i=0; i<value.dept.length; i++){
			if(typeof options == 'undefined') 
				options = objectMetadata;
		
			options['index'] = i;
			options['htmlTag'] =  'li';
%>
	<%=mw3.locateObject(value.dept[i], value.dept[i].__className, null, options, {objectId: value.__objectId, name: 'dept[' + i + ']'})%>	
<%
		}
%>
		</ul>
<%
	}
%>