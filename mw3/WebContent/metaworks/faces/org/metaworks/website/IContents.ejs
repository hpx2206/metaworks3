

<%

  if(value!=null){

	if(value.type=="p"){ //in case of paragraph
%>
<p>
<%=value.paragraph%>

<% 
	}else if(value.type=="img"){ //in case of picture
	
		if(value.file!=null){
%>

<% 
		}else{
%>

<img src="<%=value.url%>" <%=value.width >0 ? "width=" + value.width: ""%> <%=value.height >0 ? "height=" + value.height: ""%>">      

<%
		}

	}else if(value.type=="mov"){ //in case of movie
%>
<iframe width="560" height="315" src="<%=value.url%>" frameborder="0" allowfullscreen></iframe>

<% 
	}else if(value.type=="src"){ //in case of source code
%>

<pre><%=value.paragraph%></pre>

<%	 
	}else if(value.type=="file"){ //in case of file
%>

<%=fields.file.here()%>

<%	}


	var loginUser = mw3.getAutowiredObject("org.metaworks.website.IFacebookLoginUser");
	
	if(loginUser!=null && loginUser.admin){
%>
<%=methods.delete.here()%>
<%=methods.moveUp.here()%>
<%=methods.moveDown.here()%>
<%=methods.edit.here()%>
<%
	}
}
%>
