<%

//mw3.debug(value);

if(value!=null){
	if(mw3.when==mw3.WHEN_VIEW || !value.isDeleted){%>
		<b><a href="marketItemDetail?itemId=<%=value.itemId%>"><img src="<%=value.logoImageFilePath%>" ><%=value.itemName%>(ver-<%=value.version%>)</a></b>
		<div>Process By <%=value.comCode%></div>
		<p><%=value.description%></p>
	<%} else if (mw3.when==mw3.WHEN_NEW) {%>
		asdfasdfasdfadsfadf
	<%}
} else {%>
	There is no marketItem.
<%	
}
%>