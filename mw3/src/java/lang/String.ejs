<%  
 	if(value == null || !value.indexOf){
		value = "";
	}
	
	
	
	if(mw3.when == mw3.WHEN_EDIT || mw3.when == mw3.WHEN_NEW){
	
	var type = descriptor ? descriptor.getOptionValue("type") : 'text';
	
%>
	 <input id="<%=mw3.createInputId(objectId)%>" <%=type ? "type='" + type + "'" : "" %> value="<%=value%>" <%=descriptor && descriptor.attributes && descriptor.attributes.size ? " size=" + descriptor.attributes.size : ""%> style="border:1px solid #ccd; none;background:transparent">	 
<%
	}else{
%>
	 <%=value%>
<%
	}
%> 