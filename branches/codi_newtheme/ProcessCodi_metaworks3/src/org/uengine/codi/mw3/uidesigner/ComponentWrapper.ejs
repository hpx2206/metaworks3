<% if(mw3.when=='design'){%>

	<%if(value.component!=null){%>
		<%=fields.component.here()%>

	<%}else{%>
	
	drop here

<%
	 }
  }else if(mw3.when=='edit'){

  	mw3.newBeanProperty(objectId);
    
  %>

<%=mw3.locateObject(value.component, value.component.__className + ":genericfaces/ObjectFace.ejs", null, null, {objectId: objectId, name: 'component'})%>
  
  
<%

  }else{%>

<%=value.component.__className%> 

<%}%>