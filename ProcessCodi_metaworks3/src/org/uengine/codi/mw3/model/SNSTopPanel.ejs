<%
	if(value){
		if(value.loginUser){
			value.loginUser.metaworksContext.when = 'view';
		}
%>

	<div class="logo"><table cellpadding=0 cellspacing=0 border=0><td>	
    <div class="info">
        <%if(mw3.when!='__design-depth2'){/*TODO: later, rather use 'where' than 'when'.*/%>
       	 	<%=fields.loginUser.here({when:'view'})%>
        	<%=fields.session.here({when:'view'})%>
        <%}else{%>
        	session here.
        <%}%>	
	</div>	
	</td><td><img src="images/waveStyle/<%=mw3.localize("$logo")%>.png"></td></table></div>
	<%=fields.notificationBadge.here()%>
	
	<%=fields.windowPanel.here()%>	
	
	<%=fields.tray.here()%>
	
	
<%
	}
%>